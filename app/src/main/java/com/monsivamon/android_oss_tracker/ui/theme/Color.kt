package com.monsivamon.android_oss_tracker.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Primary brand colours for the OSS Tracker RE application.
 *
 * These values are used by both the light and dark colour schemes defined in
 * [AndroidossreleasetrackerTheme].
 */
val Purple200 = Color(0xFFBB86FC)    // Lighter purple — used as primary in dark theme
val Purple500 = Color(0xFF6200EE)    // Core purple   — used as primary in light theme
val Purple700 = Color(0xFF3700B3)    // Darker purple — used as tertiary in both themes
val Teal200  = Color(0xFF03DAC5)     // Teal accent   — used as secondary in both themes